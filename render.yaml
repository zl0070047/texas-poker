# Render 一键部署配置（德州扑克春节版）
# 使用方式：在 Render 后台 New → Blueprint，连接 GitHub 仓库后选择此文件

services:
  - type: web
    name: texas-holdem-spring-festival
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
